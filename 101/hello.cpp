// we add c++ into dev container -- use this command for c "gcc 101/hello.c -o hello and then run executable file as new command ./hello" and use this command for cpp "g++ 101/hello.cpp -o hello and then run executable file as new command ./hello" --

#include <iostream>

int main() {
    // Print the message to the console
    std::cout << "Hello, World!" << std::endl;
    return 0;  // Indicate that the program ended successfully
}
